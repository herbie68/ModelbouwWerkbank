<UserControl x:Class="Modelbouwer.Controls.ImageEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:language="clr-namespace:Modelbouwer.Resources.Languages"
             xmlns:local="clr-namespace:Modelbouwer.Controls">

    <Grid Margin="2 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="152" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Image -->
        <Border HorizontalAlignment="Center" VerticalAlignment="Top">
            <Border Width="150" Height="150" Style="{DynamicResource ImageBorder}">
                <Image AllowDrop="True"
                       Drop="Image_Drop"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Source="{Binding ImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <RotateTransform Angle="{Binding RotationAngle, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </Image.RenderTransform>
                </Image>
            </Border>
        </Border>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0" Background="Yellow">
            <Button Command="{Binding AddImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{DynamicResource ImageToolBarButtonStyle}"
                    ToolTip="{x:Static language:Language.ImageButtonNewTooltip}">
                <Image Style="{DynamicResource ImageToolBarAddIcon}" />
            </Button>

            <Button Command="{Binding DeleteImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{DynamicResource ImageToolBarButtonStyle}"
                    ToolTip="{x:Static language:Language.ImageButtonDeleteTooltip}">
                <Image Style="{DynamicResource ImageToolBarDeleteIcon}" />
            </Button>

            <Border Style="{DynamicResource ImageToolBarSeparatorStyle}" />

            <Button Command="{Binding RotateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{DynamicResource ImageToolBarButtonStyle}"
                    ToolTip="{x:Static language:Language.ImageButtonRotateTooltip}">
                <Image Style="{DynamicResource ImageToolBarRotateIcon}" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>