<Page x:Class="Modelbouwer.Views.CountryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Modelbouwer.Views"
      xmlns:language="clr-namespace:Modelbouwer.Resources.Languages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CountryView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="95" />   <!-- Header -->
            <RowDefinition Height="96" />   <!-- Detail fields -->
            <RowDefinition Height="40" />   <!-- Toolbar -->
            <RowDefinition Height="*" />    <!-- DataGrid -->
            <RowDefinition Height="20" />   <!-- Footer -->
        </Grid.RowDefinitions>

        <!--#region Page header -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,0"
            Background="{DynamicResource {x:Static SystemColors.InfoBrushKey}}" >
            <TextBlock Style="{DynamicResource DefaultTitle}" Text="{x:Static language:Language.metadataCountryHeaderTitel}" />
            <Canvas>
                <Path Style="{DynamicResource DefaultTitleLine}" />
            </Canvas>
            <TextBlock Style="{DynamicResource DefaultSubTitle}" Text="{x:Static language:Language.metadataCountryHeaderSubtitle}" />
        </StackPanel>
        <!--#endregion-->
    </Grid>
</Page>
